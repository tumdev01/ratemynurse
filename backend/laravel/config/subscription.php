<?php

return [
    // เปิดช่วงแรกหลังเปิดตัว — schedule การจัดการแพ็กเกจหมดอายุจะไม่ทำอะไรเลย
    // ปล่อยให้ user ใช้งานต่อได้ตามปกติแม้แพ็กเกจหมดอายุ
    'bypassed_mode' => env('BYPASSED_MODE', false),

    // เมื่อไม่ bypass แล้ว — ถ้าเปิดโหมดนี้ แพ็กเกจที่หมดอายุจะถูกปรับเป็น BASIC (ฟรี)
    // ให้อัตโนมัติแทนการตัดการใช้งาน
    'auto_free_mode' => env('AUTO_FREE_MODE', false),
];
